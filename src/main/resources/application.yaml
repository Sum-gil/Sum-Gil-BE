spring:
  application:
    name: sum-gil

  datasource:
    url: jdbc:mariadb://localhost:3307/sum_gil
    username: root
    password: 123456789
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true